# Idiolect Erasure Rate (IER) — reproducibility dependencies
# Tested with Python 3.9. Versions are lower bounds known to work.
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
torch>=2.0
transformers>=4.40
sentence-transformers>=2.2
einops>=0.7            # required by the LUAR model (rrivera1849/LUAR-MUD)
# Commercial-assistant replication (optional) uses only the Python stdlib
# (urllib) to call the OpenAI / Gemini HTTP APIs; no SDK needed.
